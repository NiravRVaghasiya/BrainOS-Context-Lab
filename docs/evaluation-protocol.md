# Evaluation protocol entry point

The evaluation protocol is maintained in [`evaluation.md`](evaluation.md). This filename is retained because the Phase 0 deliverables call for an explicit protocol document.

Before research results are reported, pin the model/provider configuration, BrainOS revision, benchmark revision, context budgets, scoring rules, and trial count. Preserve raw task-level results alongside aggregate metrics.
